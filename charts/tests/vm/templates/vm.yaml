apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: {{ include "thunder-gpu-test-vm.fullname" . | quote }}
  labels:
    {{- include "thunder-gpu-test-vm.labels" . | nindent 4 }}
    {{- with .Values.vmLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.vmAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  running: {{ .Values.running }}
  dataVolumeTemplates:
    - metadata:
        name: {{ include "thunder-gpu-test-vm.rootDiskName" . | quote }}
        labels:
          {{- include "thunder-gpu-test-vm.labels" . | nindent 10 }}
      spec:
        source:
          http:
            url: {{ .Values.dataVolume.url | quote }}
        pvc:
          accessModes:
            {{- toYaml .Values.dataVolume.accessModes | nindent 12 }}
          volumeMode: {{ .Values.dataVolume.volumeMode | quote }}
          {{- with .Values.dataVolume.storageClassName }}
          storageClassName: {{ . | quote }}
          {{- end }}
          resources:
            requests:
              storage: {{ .Values.dataVolume.storage | quote }}
  template:
    metadata:
      labels:
        {{- include "thunder-gpu-test-vm.labels" . | nindent 8 }}
    spec:
      resourceClaims:
        - name: {{ .Values.claimName | quote }}
          resourceClaimName: {{ include "thunder-gpu-test-vm.claimName" . | quote }}
      domain:
        resources:
          requests:
            memory: {{ .Values.vm.memory | quote }}
        cpu:
          cores: {{ .Values.vm.cpuCores }}
        devices:
          disks:
            - name: {{ include "thunder-gpu-test-vm.rootDiskName" . | quote }}
              disk:
                bus: virtio
            - name: cloudinitdisk
              disk:
                bus: virtio
          filesystems:
            - name: thunder-setup
              virtiofs: {}
          {{- if .Values.network.enabled }}
          interfaces:
            - name: default
              masquerade: {}
          {{- end }}
      {{- if .Values.network.enabled }}
      networks:
        - name: default
          pod: {}
      {{- end }}
      volumes:
        - name: {{ include "thunder-gpu-test-vm.rootDiskName" . | quote }}
          dataVolume:
            name: {{ include "thunder-gpu-test-vm.rootDiskName" . | quote }}
        - name: cloudinitdisk
          cloudInitNoCloud:
            userData: {{ .Values.cloudInit.userData | quote }}
        - name: thunder-setup
          secret:
            secretName: {{ include "thunder-gpu-test-vm.guestSecretName" . | quote }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
